test helmfile:
  rules:
  - changes:
    - .gitlab-ci.yml
    - helmfile-jobs.yaml
    - helmfile.yaml
  trigger:
    strategy: depend
    project: devops/samples/moon
  variables:
    CI_LIBRARY_REF_NAME: $CI_COMMIT_REF_NAME
